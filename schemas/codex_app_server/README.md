# Codex app-server schema

`codex app-server` is experimental and version-specific. After installing the standalone Codex CLI and logging in with ChatGPT, generate schemas with:

```powershell
codex app-server generate-json-schema --out schemas/codex_app_server/generated
```

Commit the generated schema files when you want to lock behavior to a known Codex version.

